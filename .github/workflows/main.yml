name: Build APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build with Buildozer
        uses: herbertopaulino/buildozer-action@master
        with:
          command: buildozer android debug
          
